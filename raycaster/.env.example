# ElevenLabs API key used by the in-browser AudioManager fallback path and by
# tools/audio/bake_sound_bank.mjs. Prefer baking a sound bank offline so the
# key never ships to browsers.
VITE_ELEVENLABS_API_KEY=
ELEVENLABS_API_KEY=
