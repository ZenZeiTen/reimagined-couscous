Drop baked sprite sheets here (`<name>.png` + `<name>.json`) from
`tools/blender/sprite_baker.py`. The demo looks for `grunt.json` and
`brute.json` and falls back to procedural sheets when they are absent.
